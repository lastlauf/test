# Optional: enable "Sign in with Google". Leave unset to use username accounts only.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Public origin used to build the Google OAuth redirect URI (default http://localhost:3000)
APP_ORIGIN=http://localhost:3000

# Where the SQLite database lives (default ./data/tsi.db)
TSI_DB_PATH=
